'use client'

export default function GlobalPresence() {
  return (
    <section id="presence" className="py-8 md:py-12 bg-primary-950">
      <div className="container-custom">
        <div className="h-px bg-gradient-to-r from-transparent via-primary-700/50 to-transparent"></div>
      </div>
    </section>
  )
}
